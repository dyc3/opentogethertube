name: "OpenTogetherTube"
tasks:
  start:
    command: npm run dev
    inboundUrl: http://localhost:3000
  api-server:
    command: npm run api-server
    inboundUrl: http://localhost:3000
  tests:
    command: npm run test
    inboundUrl: http://localhost:3000