[package]
name = "harness-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
harness.workspace = true

[[test]]
name = "integration"
path = "src/main.rs"
harness = false # disable the default libtest harness
