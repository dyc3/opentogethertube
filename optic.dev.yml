name: "OpenTogetherTube"
tasks:
  start:
    command: yarn run dev
    inboundUrl: http://localhost:3000
  api-server:
    command: yarn run api-server
    inboundUrl: http://localhost:3000
  tests:
    command: yarn run test
    inboundUrl: http://localhost:3000